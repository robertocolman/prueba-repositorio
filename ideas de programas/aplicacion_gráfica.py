#ideas

#def programagrafico():
import tkinter

ventana = tkinter.Tk() #crear ventana
ventana.geometry("300x300") #tamaño de la ventana
return programagrafico()

#etiqueta = tkinter.Label(ventana, text = "hola mundo") #agregar etiqueta
# etiqueta.pack() #ejecutar la etiqueta en el programa
# etiqueta.pack(side = tkinter.BOTTOM) para que el hola mundo vaya abajo de todo
# etiqueta.pack(side = tkinter.RIGHT) para que el hola mundo vaya a la derecha
# etiqueta.pack(fill = tkinter.Y, expand = True) para que sea vertical
# etiqueta.pack(fill = tkinter.BOTH, expand = True) para que la etiqueta ocupe toda la pantalla
#etiqueta.pack(fill = tkinter.X) #el texto abarca toda la barra 

#def saludo(nombre):
#	print("hola" + nombre)

#boton1 = tkinter. Button(ventana, text = "presiona", padx = 10, pady = 20, command = lambda: saludo(" python")) #agregar un boton, coomand es para invocar la funcion, lambda ejecuta un texto dentro de la funcion


#boton1.pack()

#ventana.mainloop() #registro de todo lo que se ve en la ventana

#boton1 = tkinter.Button(ventana, text = "boton1", width = 10, height = 5)
#boton2 = tkinter.Button(ventana, text = "boton2", width = 10, height = 5)
#boton3 = tkinter.Button(ventana, text = "boton3", width = 10, height = 5)

#boton1.grid(row = 0, column = 0) #define la posición del boton
#boton2.grid(row = 1, column = 0)
#boton3.grid(row = 2, column = 0) 
#ventana.mainloop()